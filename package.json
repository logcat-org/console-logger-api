{
  "name": "console-logger-api",
  "version": "1.0.2",
  "repository": {
    "type": "git",
    "url": "git://github.com/spirinvladimir/console-logger-api.git"
  },
  "scripts": {
    "test": "node -e \"require('./index').error('should be red')\"",
    "start": "npm run watch",
    "build": "lein cljsbuild once",
    "watch": "lein cljsbuild auto"
  }
}
